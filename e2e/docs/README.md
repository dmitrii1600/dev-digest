# e2e/docs — suite-local explanations

Explanations that belong only to the browser suite: agent-browser command notes,
flakiness triage, CI artifact handling.

The flow format, env knobs and coverage table stay in `../README.md`.

Note: `../specs/` in this package holds **agent-browser flow specs**
(`NN-name.flow.json`), not feature specifications. Feature specs for the web app
go in `../../client/specs/` or `../../specs/`.
