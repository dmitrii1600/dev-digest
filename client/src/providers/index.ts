/* The app-wide provider stack. Forwarding file, one export — see
   .claude/skills/frontend-ui-architecture/references/boundaries.md on why this
   is a forward and not an aggregating barrel. */
export { Providers } from "./AppProviders";
