# client/specs — studio feature specifications

Specs for features that live entirely inside `@devdigest/web` (a new screen, a
new panel, an interaction change). Features that also change the API go in
`../../specs/`.

Format and lifecycle: see [`../../specs/README.md`](../../specs/README.md).
