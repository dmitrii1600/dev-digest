# reviewer-core/docs — engine-local explanations

Explanations that belong only to the engine: prompt-slot semantics, structured
output and repair behaviour, grounding rules in detail, map-reduce strategy.

The pipeline overview and public API stay in `../README.md`. Prompt-authoring
conventions live in `../../docs/agent-prompts/` — link, do not duplicate.

When you add a document, link it from `../CLAUDE.md` (`## Read when`) with the
condition under which an agent should open it.
