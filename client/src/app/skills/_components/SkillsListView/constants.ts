/** Card grid template — same responsive auto-fill as the Agents list
 *  (`agents/_components/AgentsListView/constants.ts`). */
export const CARD_GRID_COLS = "repeat(auto-fill, minmax(280px, 1fr))";
