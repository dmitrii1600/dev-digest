# server/specs — API feature specifications

Specs for features that live entirely inside `@devdigest/api` (a new module, a
new adapter, a schema change). Cross-package features go in `../../specs/`.

Format and lifecycle: see [`../../specs/README.md`](../../specs/README.md).
