# server/docs — API-local explanations

Explanations that are too detailed for `../README.md` and belong only to the
server: adapter contracts, DB design notes, operational runbooks, decision
records.

Architecture overview, the request/DI flow and the route map stay in
`../README.md` — it is the source of truth. Do not duplicate it here.

Cross-package documents go in `../../docs/`.

When you add a document, link it from `../AGENTS.md` (`## Read when`) with the
condition under which an agent should open it.
