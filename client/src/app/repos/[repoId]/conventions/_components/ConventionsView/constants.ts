/** Constants for the Conventions page. */

/** Error codes the extract route returns that get their own copy on the page. */
export const EXTRACT_ERROR_COPY: Record<string, string> = {
  repo_not_indexed: "page.notIndexed",
  repo_not_cloned: "page.notCloned",
  scan_running: "page.scanRunning",
};

/** How wide the Create-skill modal opens. */
export const MODAL_WIDTH = 860;
