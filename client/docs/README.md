# client/docs — studio-local explanations

Explanations that belong only to the web app: interaction patterns, shell and
navigation decisions, design notes for a screen family.

The route map and the API surface each page uses stay in `../README.md` — it is
the source of truth. Do not duplicate it here.

Cross-package documents go in `../../docs/`.

When you add a document, link it from `../AGENTS.md` (`## Read when`) with the
condition under which an agent should open it.
