import type { SkillType } from "@devdigest/shared";

/** Type badge order (also drives the create/import type <select>). */
export const SKILL_TYPES: readonly SkillType[] = ["rubric", "convention", "security", "custom"];
