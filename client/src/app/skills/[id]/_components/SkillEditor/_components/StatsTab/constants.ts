/** Cycling palette for the by-category donut — categories are finding
 *  categories (open-ended, come from the model), not a fixed enum, so there
 *  is no single canonical color per category to key off. */
export const CATEGORY_PALETTE: readonly string[] = [
  "var(--accent)",
  "var(--ok)",
  "var(--warn)",
  "var(--crit)",
  "var(--sugg)",
  "var(--info)",
];
